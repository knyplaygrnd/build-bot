# This file contains environment variables for configuring the Build Bot.
# Please fill in the required values and uncomment optional settings as needed.

# --- Telegram Notification Settings ---
# CONFIG_BOT_TOKEN: Your Telegram Bot API Token. Obtain this from BotFather.
CONFIG_BOT_TOKEN="your_telegram_bot_token"
# CONFIG_CHATID: The chat ID where the bot will send notifications. Can be a user ID or group ID.
CONFIG_CHATID="your_telegram_chat_id"
# CONFIG_ERROR_CHATID (Optional): A separate chat ID for sending error logs. If left commented, CONFIG_CHATID will be used for errors.
# CONFIG_ERROR_CHATID="your_error_chat_id"

# --- General Build Settings ---
# CONFIG_JOBS (Optional): Number of parallel threads/jobs for compilation (e.g., 8, 16).
# If commented, the script will automatically detect and use all available CPU cores.
# CONFIG_JOBS="8"

# --- ROM Build Specific Settings ---
# CONFIG_ROM_NAME (Optional): A custom name for your ROM. If commented, the current directory name will be used.
# CONFIG_ROM_NAME="My Bomb ROM"
# CONFIG_DEVICE: The codename of the device you are building for (e.g., "spes", "fog").
CONFIG_DEVICE="your_device_codename"
# CONFIG_BUILD_TARGET: The build target for your ROM (e.g., "bacon", "pixelos").
CONFIG_BUILD_TARGET="your_rom_build_target"
# CONFIG_BUILD_TYPE: The type of build (e.g., "userdebug", "user", "eng").
CONFIG_BUILD_TYPE="userdebug"
# CONFIG_RECOVERY_IMAGES (Optional): A semicolon-separated list of image files to package as recovery (e.g., "recovery.img;dtbo.img;vendor_boot.img").
# CONFIG_RECOVERY_IMAGES="recovery.img;dtbo.img"
# CONFIG_ROM_CUSTOM_COMMANDS (Optional): Additional shell commands to execute during the ROM build process.
# Example: export IS_OFFICIAL=true; make clean;
# CONFIG_ROM_CUSTOM_COMMANDS="export IS_OFFICIAL=true"

# --- Kernel Build Specific Settings ---
# CONFIG_DEFCONFIG: The defconfig file to use for building the kernel (e.g., "vendor/spes-perf_defconfig").
CONFIG_DEFCONFIG="your_kernel_defconfig"
# CONFIG_AK3_REPO: The Git repository URL for AnyKernel3 (e.g., "https://github.com/osm0sis/AnyKernel3.git").
CONFIG_AK3_REPO="your_anykernel3_repo_url"
# CONFIG_FILES_MAP (Optional): Custom mapping of kernel output files to AnyKernel3 directory.
# Format: "source_file1:destination_file1;source_file2:destination_file2"
# Example: Image.lz4:Image.lz4;dtbo.img:dtbo.img
# CONFIG_FILES_MAP="Image.lz4:Image.lz4;dtbo.img:dtbo.img"
# CONFIG_KSU_URL (Optional): URL for KernelSU setup script. Uncomment if building with KernelSU.
# CONFIG_KSU_URL="https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh"
# CONFIG_KERNEL_CUSTOM_COMMANDS (Optional): Additional shell commands to execute during the kernel build process.
# Example: echo CONFIG_KSU_TAMPER_SYSCALL_TABLE=y >> out/.config
# CONFIG_KERNEL_CUSTOM_COMMANDS="echo CONFIG_KSU_TAMPER_SYSCALL_TABLE=y >> out/.config"

# --- Upload Settings ---
# CONFIG_PDUP_API: Your PixelDrain API key. Required for PixelDrain uploads.
CONFIG_PDUP_API="your_pixeldrain_api_key"
# CONFIG_GOFILE: Set to "true" to enable GoFile uploads in addition to PixelDrain.
CONFIG_GOFILE="true"